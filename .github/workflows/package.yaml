name: "package"

on:
  pull_request:
  push:
    branches:
      - main
      - develop
      - "release/**"
    paths:
      - "**.dart"
      - "./*.yaml"
      - .github/workflows/_base-dart.yaml
      - .github/workflows/package.yaml
  release:
    types: [published]

# call the base workflow with the package path
jobs:
  dart:
    uses: kerberjg/dart_flutter_ci_experiments/.github/workflows/pipeline.yaml@5499d308241da53d0b175b1a78e16056a97aa918
    strategy:
      matrix:
        package:
          - crystallis
          - crystallis_generator
          - crystallis_plugin
    name: ${{ matrix.package }}
    with:
      path: "packages/${{ matrix.package }}"
      force-sdk: dart
      min-publish-score: 150
