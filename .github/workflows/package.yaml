name: "package"

on:
  pull_request:
  push:
    branches:
      - main
      - develop
      - "release/**"
    paths:
      - "**.dart"
      - "./*.yaml"
      - .github/workflows/_base-dart.yaml
      - .github/workflows/package.yaml
  release:
    types: [published]

# call the base workflow with the package path
jobs:
  dart:
    uses: kerberjg/dart_flutter_ci_experiments/.github/workflows/pipeline.yaml@1005fbe057d300105490ff04956178d4c28c06be
    strategy:
      matrix:
        package:
          - crystallis
          - crystallis_generator
          - crystallis_plugin
    name: ${{ matrix.package }}
    with:
      path: "packages/${{ matrix.package }}"
      force-sdk: dart
      min-publish-score: 150
      skip-publish: ${{ !(github.event_name == 'release') }}
